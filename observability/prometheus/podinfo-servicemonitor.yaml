apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: podinfo-servicemonitor
  namespace: dev
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: podinfo
  namespaceSelector:
    matchNames:
      - dev
  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s
